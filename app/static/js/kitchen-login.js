document.querySelector("#kitchen-login-form")?.addEventListener("submit",async(event)=>{event.preventDefault();const form=event.currentTarget;const data=new FormData(form);const box=document.querySelector("#login-error");box.hidden=true;const response=await fetch("/auth/kitchen/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:data.get("email"),password:data.get("password")})});if(response.ok){window.location.assign("/kitchen");return}box.textContent="Email ou senha inválidos.";box.hidden=false;});
